import { TodoModel } from "../model/todo.model";

const TodoData: TodoModel[] = [
    new TodoModel("Buy milk"),
    new TodoModel("Buy milk2"),
    new TodoModel("Buy milk3"),
]

export default TodoData;